# Address of the FastAPI backend used for proxy rewrites
BACKEND_URL=http://localhost:8000

# Optional: directly point the browser fetch call to the backend
# If unset, the app will call /api/generate which Next.js rewrites using BACKEND_URL
NEXT_PUBLIC_BACKEND_URL=
